origin_resource = "subscription"

[[create_resource]]
resource_type = "network"
relation_type = "BASELINE_TEMPLATE"
name = "dev-network"
[create_resource.properties]
function = "{{- blueprint -}}"
environment = "dev"
cidr = "10.37.196.0/24"
description = "A network segment is a portion of a computer network that's separated from the rest, either physically or logically. It behaves as a distinct, self-contained broadcast or addressing domain."
created = "{{- now(utc=true) | date(format='%Y-%m-%dT%H:%M:%SZ') -}}"

[[create_resource]]
resource_type = "network"
relation_type = "BASELINE_TEMPLATE"
name = "int-network"
[create_resource.properties]
function = "{{- blueprint -}}"
environment = "int"
cidr = "10.37.197.128/27"
description = "A network segment is a portion of a computer network that's separated from the rest, either physically or logically. It behaves as a distinct, self-contained broadcast or addressing domain."
created = "{{- now(utc=true) | date(format='%Y-%m-%dT%H:%M:%SZ') -}}"

[[create_resource]]
resource_type = "network"
relation_type = "BASELINE_TEMPLATE"
name = "prod-network"
[create_resource.properties]
function = "{{- blueprint -}}"
environment = "prod"
cidr = "10.37.197.160/27"
description = "A network segment is a portion of a computer network that's separated from the rest, either physically or logically. It behaves as a distinct, self-contained broadcast or addressing domain."
created = "{{- now(utc=true) | date(format='%Y-%m-%dT%H:%M:%SZ') -}}"
