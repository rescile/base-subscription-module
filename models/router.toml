origin_resource = "region"

provider_name = { "function!" = "{{- origin_resource.name | regexp(expr='s/^([^-]+)-.*/\\1/') | lower -}}" }
resource_id = { "json!" = "dictionary.json", jmespath = "values(@)[?name == provider_name ] | [0]" }

[[create_resource]]
resource_type = "router"
relation_type = "SATISFIES"
name = "{{- origin_resource.provider.name | lower -}}-{{- origin_resource.code | lower -}}-{{- resource_id.router -}}"
[create_resource.properties]
function = "{{- origin_resource.function | lower -}}"
pid = "{{- resource_id.router -}}"
provider = "{{- origin_resource.provider -}}"
region = "{{- origin_resource.name -}}"
cidr = "10.0.0.0/19"
bgp = ["172.16.0.1/30"]
vpn = []
description = "The {{ origin_resource.provider.display }} {{ origin_resource.function }} router is a virtual appliance that analyzes incoming data packets, determines the best path for them to travel across interconnected networks, and forwards them toward their intended destination."
created = "{{- now(utc=true) | date(format='%Y-%m-%dT%H:%M:%SZ') -}}"

origin_resource = "router"

[[link_resources]]
with = "subscription"
join = { local = "provider", remote = "provider" }
create_relation = { type = "SATISFIES" }
copy_properties = [
  { from = "account", as = "network" }
]
