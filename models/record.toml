origin_resource = "zone"

[[create_resource]]
create_from = { property = "record" }
relation_type = "DERIVED_FROM"
name = "{{- property.value -}}.{{- origin_resource.name -}}"
[create_resource.properties]
function = "A"
created = "{{- now(utc=true) | date(format='%Y-%m-%dT%H:%M:%SZ') -}}"
