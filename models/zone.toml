origin_resource = "resolver"

provider_name = { "function!" = "{{- origin_resource.name | regexp(expr='s/^([^-]+)-.*/\\1/') | lower -}}" }
resource_id = { "json!" = "dictionary.json", jmespath = "values(@)[?name == provider_name ] | [0]" }

[[create_resource]]
resource_type = "zone"
relation_type = "DEPENDS_ON"
name = "{{ origin_resource.name | regexp(expr='s/^([^-]+)-.*/\\1/') | lower }}-private-{{- resource_id.zone -}}"
[create_resource.properties]
function = "private"
pid = "{{- resource_id.zone -}}"
domain = "internal.utils.com"
description = "A DNS zone is an administrative namespace that a single entity manages, containing resource records that map domain names to IP addresses."
created = "{{- now(utc=true) | date(format='%Y-%m-%dT%H:%M:%SZ') -}}"

[[create_resource]]
create_from = { property = "zone" }
relation_type = "DEPENDS_ON"
name = "{{ origin_resource.name | regexp(expr='s/^([^-]+)-.*/\\1/') | lower }}-public-{{- resource_id.zone -}}"
[create_resource.properties]
function = "public"
pid = "{{- resource_id.zone -}}"
domain = "{{- property.value | lower -}}"
description = "A DNS zone is an administrative namespace that a single entity manages, containing resource records that map domain names to IP addresses."
created = "{{- now(utc=true) | date(format='%Y-%m-%dT%H:%M:%SZ') -}}"

origin_resource = "zone"

[[link_resources]]
with = "subscription"
match_on = [ { property = "function", value = "public" } ]
create_relation = { type = "SATISFIES" }
copy_properties = [
  { from = "account", as = "record" }
]

[[link_resources]]
with = "subscription"
match_on = [ { property = "function", value = "private" } ]
create_relation = { type = "SATISFIES" }
copy_properties = [
  { from = "util", as = "record" }
]
