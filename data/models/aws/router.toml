# This model represents a third tier model of a cloud domain and defines routers where provider resources become accessible.
origin_resource = "region"

# Create the resource type
[[create_resource]]
create_from = { property = "router"}
relation_type = "CONNECTS_TO"
[create_resource.properties]
function = "transit"
provider = "{{- origin_resource.provider -}}"
region = "{{- origin_resource.name -}}"
ipv6 = false
description = "The transit router in {{ location | capitalize }} is a regional network function that analyzes incoming data packets and forwards them toward their intended destination."
created = "{{- now(utc=true) | date(format='%Y-%m-%dT%H:%M:%SZ') -}}"
