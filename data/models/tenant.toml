[[create_resource]]
resource_type = "tenant"
relation_type = "DEFINED_BY"
name = "{{- tenant | lower -}}"
[create_resource.properties]
description = "The tenant represents the organization that has private, secure access to the dedicated set of computing resources and services within the cloud infrastructure. Essentially, it is the technical term for the customer's dedicated partition where their data, applications, and virtual machines reside, isolated from other cloud customers."
created = "{{- now(utc=true) | date(format='%Y-%m-%dT%H:%M:%SZ') -}}"
