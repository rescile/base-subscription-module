origin_resource = "resident"

[[create_resource]]
resource_type = "registry"
relation_type = "BASELINE_TEMPLATE"
name = "key-registry"
[create_resource.properties]
function = "infrastructure"
domain = "core"
source_ip = ["192.168.1.50/32", "10.0.0.0/24"]
description = "The master key vault stores main keys for BYOK internally."
created = "{{- now(utc=true) | date(format='%Y-%m-%dT%H:%M:%SZ') -}}"
